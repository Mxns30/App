rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 임시로 모든 읽기 허용 (개발용)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
} 